<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de Producto - {{product.name}}</title>
</head>
<body>
    <h1>Detalle del Producto</h1>

    <div class="product-detail">
        <h2>{{product.name}}</h2>
        <p><strong>Descripción:</strong> {{product.description}}</p>
        <p><strong>Precio:</strong> ${{product.price}}</p>
        <p><strong>Categoría:</strong> {{product.category}}</p>
        <p><strong>Disponibilidad:</strong> {{product.availability}}</p>
        
        <!-- Botón para agregar al carrito -->
        <form action="/api/carts/{{cartId}}/product/{{product.id}}" method="POST">
            <button type="submit">Agregar al carrito</button>
        </form>
    </div>

    <!-- Enlace para volver a la lista de productos -->
    <a href="/products">Volver a la lista de productos</a>
</body>
</html>
